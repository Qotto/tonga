[tox]
envlist = py37
skipsdist = True

[testenv]
commands =
    pip install pipenv
    pipenv install --dev
    rm -fr .mypy_cache
    mypy aioevent
    pytest
whitelist_externals =
    pip
    pipenv
    pytest
    make
    mypy
    rm
